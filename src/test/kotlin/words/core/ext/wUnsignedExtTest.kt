package words.core.ext

import ForthTestCase
import kf.words.core.ext.wUnsignedExt

class wUnsignedExtTest : ForthTestCase() {
    val mod = wUnsignedExt

    // all funcs right now point to non-unsigned versions
}