package words.core

import ForthTestCase
import kf.words.core.wUnsigned
import org.junit.jupiter.api.Assertions.*

class wUnsignedTest : ForthTestCase() {
    val mod = wUnsigned

    // until there are really unsigned funcs, these are covered by the
    // non-unsigned funcs they point to.
}